name: zweidenker_heinzelmen
description: A Flutter Package with useful helpers that are used across Flutter Apps by ZWEIDENKER
version: 0.0.2
repository: https://github.com/zweidenker/flutter-heinzelmen/packages/zweidenker_heinzelmen

environment:
  sdk: ">=2.16.2 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_web_browser: ^0.17.1
  package_info_plus: ^1.4.2
  universal_platform: ^1.0.0+1
  url_launcher: ^6.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
  mocktail: ^0.3.0
  plugin_platform_interface: ^2.1.2
  url_launcher_platform_interface: ^2.0.5

flutter:

